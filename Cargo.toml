[package]
name = "copper"
version = "0.1.0"
edition = "2024"

[dependencies]
libcopper = { version = "0.1.0", path = "libcopper" }
strum = { version = "0.28.0", features = ["derive"] }
strum_macros = "0.28.0"
